# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=your_postgres_password
DB_NAME=electus

# App Configuration
PORT=3000

# Optional: Microservice URLs (Defaults are used if left blank)
# AI_SERVICE_URL=http://localhost:3002
# DOCUMENT_SERVICE_URL=http://localhost:3003

# Optional: SMTP Configuration (Falls back to Ethereal fake SMTP if not provided)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_SECURE=false
# SMTP_USER=your_email@gmail.com
# SMTP_PASS=your_app_password
# SMTP_FROM_NAME=Electus ATS
